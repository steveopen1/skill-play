---
name: api-security-testing
description: API安全测试引导框架。【触发词】安全测试、安全审计、渗透测试、漏洞检测、安全评估、api安全、接口安全、帮我检测漏洞、检查安全问题、安全报告。【核心理念】Skill是引导者而非自动化工具 - 提供决策树、检查清单、推理引导、知识库，辅助人类测试者而非替代。【重要】必须确认用户拥有该目标的合法授权！
---

## 核心理念

### 定位转变

```
❌ 过去（错误认知）
   Skill = 全自动化测试工具
   Skill = "一键扫描"脚本
   Skill = 替代人类测试者

✅ 现在（正确认知）
   Skill = 经验丰富的安全顾问
         = 决策流程引导者
         = 测试思路提供者
         = 辅助人类测试者的框架
```

### 核心原则

**1. Skill 是引导，不是执行**
- 提供决策树，而不是自动执行
- 给出检查清单，而不是测试脚本
- 提供思考方向，而不是给出结论

**2. 不确定性是人类的价值**
- 安全测试充满不确定性
- 人类判断比自动化更灵活
- 经验直觉比规则更重要

**3. Skill 辅助人类，而不是替代**
- 工具集成由人控制
- 测试过程由人决策
- 最终结论由人判断

---

## 核心检测思维

### 遇到"查询类"接口时

```
思考：这个接口查的是什么数据？需要认证吗？能查到别人的数据吗？
```

**推理步骤：**
1. 这个接口查询需要什么参数？（userId、phone、orderNo...）
2. 不带参数能查到数据吗？
3. 带别人的ID能查到数据吗？（IDOR）
4. 响应中有没有敏感字段？（password、token、余额...）

**示例：**
```
你发现：GET /api/user/info?userId=123
思考：
  - 需要认证吗？→ 测试不带token
  - 能查其他用户吗？→ 测试userId=124
  - 响应有敏感字段吗？→ 检查password、token等
```

### 遇到"认证类"接口时

```
思考：认证机制安全吗？能绕过吗？能枚举用户吗？
```

**推理步骤：**
1. 不带认证信息能访问吗？
2. 伪造token能通过吗？（JWT alg:none）
3. 用户不存在时的响应有区别吗？（用户枚举）
4. 有短信验证码吗？能轰炸吗？

### 遇到"资金/订单类"接口时

```
思考：钱能转走吗？订单能篡改吗？能刷单吗？
```

**推理步骤：**
1. 订单归属校验了吗？（用A的token能操作B的订单吗？）
2. 金额能篡改吗？（改成0.01）
3. 退款接口需要什么权限？能绕过吗？

### 遇到"用户信息"接口时

```
思考：别人的资料能拿到吗？密码暴露了吗？能修改吗？
```

**推理步骤：**
1. 不带token能拿到吗？
2. 响应里有password吗？
3. 能通过phone/email找到userId吗？
4. 修改接口有校验吗？能改别人的吗？

---

## 敏感信息识别

### 必须识别这些敏感字段

```
password      → 不应返回前端
token         → 可能存在泄露
secretKey     → 不应暴露
apiKey        → 不应暴露
balance       → 可能存在越权
orderNo       → 可能被篡改
userId        → 可用于越权测试
phone         → 可用于用户枚举
email         → 可用于钓鱼
```

### 响应类型分类

| 响应类型 | 特征 | 含义 |
|----------|------|------|
| JSON对象 | `{"code":200,"data":{...}}` | 真实API响应 |
| JSON数组 | `[{"id":1,...}]` | 真实数据列表 |
| HTML页面 | `<!DOCTYPE html>...` | SPA路由/WAF/错误页 |
| 空响应 | 长度<50字节 | 可能是错误/空数据 |
| 重定向 | HTTP 301/302 | 需要认证/跳转 |

### 响应分析思维

当你看到响应时：
```
1. 这个响应正常吗？ → 检查状态码
2. 有敏感字段吗？ → 搜索password/token/secret
3. 有ID类字段吗？ → 尝试遍历
4. 有手机号吗？ → 尝试用户枚举
5. 有订单号吗？ → 尝试越权操作
```

---

## 决策树引导系统

### 决策树索引

| 场景 | 决策树 | 说明 |
|------|--------|------|
| 发现登录/认证端点 | [认证端点决策树](#认证端点决策树) | 分析认证机制安全性 |
| 发现API端点 | [API端点决策树](#api端点决策树) | 判断端点类型和测试重点 |
| 发现文件上传 | [文件上传决策树](#文件上传决策树) | 分析上传功能风险 |
| 响应全是HTML | [SPA识别决策树](#spa应用决策树) | 判断是否需要JS采集 |
| 所有请求被拦截 | [WAF识别决策树](#waf识别决策树) | 判断是否为安全设备 |

### 认证端点决策树

```
## 当发现登录/认证端点时

### 第一步: 收集基础信息
- [ ] 查看登录表单HTML，获取参数名
- [ ] 拦截真实的登录请求
- [ ] 分析请求头（Cookie、Token、Referer等）
- [ ] 检查是否有JWT token及其签名算法

### 第二步: 分析响应特征
- [ ] 测试空密码的响应
- [ ] 测试错误密码的响应
- [ ] 测试不存在用户的响应
- [ ] 对比错误消息是否有差异

### 第三步: 推理可能的漏洞
如果错误消息有差异 → 可能存在用户枚举
如果返回SQL错误 → 可能存在SQL注入
如果响应时间不同 → 可能存在时序攻击
如果有验证码 → 需要处理验证码

### 第四步: 选择测试策略
→ 参考"检查清单 - 认证测试"
→ 选择合适的测试payload
→ 记录测试过程
```

### API端点决策树

```
## 当发现API端点时

### 第一步: 判断端点类型
根据URL路径判断:
- /login, /auth, /token → 认证类
- /user, /profile → 用户类
- /admin, /manage → 管理类
- /file, /upload → 文件操作类
- /api/config, /settings → 配置类

### 第二步: 选择测试重点
认证类 → SQL注入、暴力破解、用户枚举
用户类 → IDOR、信息泄露
管理类 → 越权、权限绕过
文件类 → 上传绕过、路径遍历
配置类 → 信息泄露

### 第三步: 执行测试
→ 参考"检查清单 - {类型}测试"
→ 记录发现的证据
→ 验证是否为误报
```

### 文件上传决策树

```
## 当发现文件上传接口时

### 第一步: 分析上传功能
- [ ] 确定上传参数名（file, image, attachment...）
- [ ] 分析允许的文件类型
- [ ] 查看上传后的访问URL

### 第二步: 识别验证位置
客户端验证？ → 可绕过
服务端验证？ → 需要分析验证逻辑

### 第三步: 测试绕过方式
- [ ] 双重扩展名（.php.jpg）
- [ ] MIME类型绕过
- [ ] 文件内容欺骗
- [ ] 路径遍历上传

### 第四步: 验证利用
→ 尝试访问上传后的文件
→ 确认是否可执行
```

### SPA应用决策树

```
## 当所有API返回HTML时

### 第一步: 确认是SPA
- [ ] 检查响应内容是否相同
- [ ] 查看是否有Vue/React特征
- [ ] 检查响应长度

### 第二步: 判断处理方式
如果是SPA：
→ 需要从JS中提取真实API地址
→ 需要使用浏览器工具触发动态API

如果不是SPA：
→ 检查是否为WAF拦截
→ 检查是否需要特定Header
```

### WAF识别决策树

```
## 当请求被统一拦截时

### 第一步: 识别WAF特征
- [ ] 响应包含"拦截"、"安全"、"访问受限"
- [ ] 所有响应返回相似的HTML页面

### 第二步: 验证WAF
发送正常请求对比：
- 请求1: 返回业务JSON = 正常
- 请求2: 返回HTML拦截页 = WAF
- 请求3: 返回业务JSON = 恢复

### 第三步: 处理策略
确认为WAF后：
→ 记录"存在WAF防护"
→ 可尝试降低请求频率
→ 不是漏洞，是安全能力
```

---

## 检查清单系统

> **重要**：以下检查清单为**示例**，包括但不限于所列内容。实际测试中应根据目标特性灵活扩展。

### 检查清单索引

| 检查清单 | 适用场景 |
|----------|----------|
| [认证测试检查清单](#认证测试检查清单) | 登录/认证接口 |
| [SQL注入检查清单](#sql注入检查清单) | 所有用户输入点 |
| [IDOR检查清单](#idor检查清单) | 资源访问接口 |
| [JWT检查清单](#jwt检查清单) | 使用JWT的接口 |
| [敏感信息检查清单](#敏感信息检查清单) | 所有接口响应 |
| [文件上传检查清单](#文件上传检查清单) | 文件上传接口 |

### 认证测试检查清单

> **示例**，包括但不限于以下测试项：

```
## 认证端点测试清单（示例）

### 基础信息收集
- [ ] 记录完整的登录URL
- [ ] 记录所有请求参数名
- [ ] 拦截真实的登录请求
- [ ] 分析请求头（Cookie、Token、Referer等）
- [ ] 检查是否有JWT token及其签名算法
- [ ] （其他：根据目标特性扩展）

### 弱密码测试（示例）
- [ ] admin:admin
- [ ] admin:123456
- [ ] admin:admin123
- [ ] （其他常见弱密码：根据目标特性和行业特点扩展）

### SQL注入测试（示例）
- [ ] ' OR '1'='1
- [ ] admin'--
- [ ] ' OR 1=1--
- [ ] （其他payload：根据数据库类型扩展）

### 用户枚举测试（示例）
- [ ] 测试存在的用户名
- [ ] 测试不存在的用户名
- [ ] 对比响应差异
- [ ] （其他：手机号、邮箱等）

### 手动验证
- [ ] 上述测试是否触发了账户锁定？
- [ ] 是否有验证码？如何处理？
- [ ] 是否有其他认证方式？
```

### SQL注入检查清单

> **示例**，包括但不限于以下测试项：

```
## SQL注入测试清单（示例）

### 注入点识别（示例）
- [ ] URL参数中的ID
- [ ] POST body参数
- [ ] Header参数
- [ ] Cookie参数
- [ ] （其他：JSON参数、GraphQL query等）

### 布尔盲注测试（示例）
- [ ] ' OR '1'='1
- [ ] ' AND '1'='1
- [ ] ' OR 1=1--
- [ ] ' AND 1=2--
- [ ] （其他：根据目标数据库扩展）

### 时间盲注测试（示例）
- [ ] ' AND SLEEP(5)--
- [ ] ' AND (SELECT COUNT(*) FROM information_schema.tables) > 0--
- [ ] （其他：根据数据库类型选择延时函数）

### 报错注入测试（示例）
- [ ] ' AND EXTRACTVALUE(1, CONCAT(0x7e, DATABASE()))
- [ ] ' AND updatexml(1, CONCAT(0x7e, VERSION()))
- [ ] （其他：UNION SELECT等）

### 验证记录
- [ ] 响应时间差异
- [ ] SQL错误消息
- [ ] 响应内容差异
```

### IDOR检查清单

> **示例**，包括但不限于以下测试项：

```
## IDOR越权测试清单（示例）

### 资源标识识别（示例）
- [ ] userId
- [ ] orderId
- [ ] accountId
- [ ] （其他：any predictable ID如orderNo, ticketId等）

### 水平越权测试（示例）
- [ ] 用用户A的token访问用户B的资源
- [ ] 修改请求中的ID为他人ID
- [ ] 测试ID可遍历范围
- [ ] （其他：根据业务特性扩展）

### 垂直越权测试（示例）
- [ ] 普通用户token访问管理员接口
- [ ] 测试权限提升操作
- [ ] （其他：不同角色间的权限测试）

### 验证记录
- [ ] 是否返回了他人数据
- [ ] 响应是否有权限提示
- [ ] 是否需要原用户token
```

### JWT检查清单

> **示例**，包括但不限于以下测试项：

```
## JWT测试清单（示例）

### Token结构分析
- [ ] 提取JWT token
- [ ] 分析Header（alg字段）
- [ ] 分析Payload（敏感字段检查）
- [ ] （其他：exp过期时间、iss发行者等）

### 签名算法攻击（示例）
- [ ] alg: none → 删除签名部分
- [ ] alg: HS256 → 使用公钥签名
- [ ] （其他：算法混淆、密钥混淆等）

### 敏感信息检查（示例）
- [ ] payload中是否包含password？
- [ ] payload中是否包含secret？
- [ ] payload中是否包含apiKey？
- [ ] （其他：role、permission等权限相关字段）

### Token滥用测试（示例）
- [ ] token是否有过期时间？
- [ ] token是否可以重复使用？
- [ ] 修改userId后token是否有效？
- [ ] （其他：token刷新机制等）
```

### 敏感信息检查清单

> **示例**，包括但不限于以下检查项：

```
## 敏感信息泄露检查清单（示例）

### 认证信息（示例）
- [ ] password
- [ ] token
- [ ] secretKey
- [ ] apiKey
- [ ] （其他：privateKey, accessKey, secret等）

### 业务数据（示例）
- [ ] balance（余额）
- [ ] orderNo（订单号）
- [ ] phone（手机号）
- [ ] email（邮箱）
- [ ] idCard（身份证）
- [ ] （其他：银行卡号、地址、年龄等敏感个人信息）

### 内部信息（示例）
- [ ] 内网IP
- [ ] 数据库连接信息
- [ ] 内部系统URL
- [ ] 调试接口
- [ ] （其他：JWT密钥、OSS配置、云服务凭证等）

### 配置文件（示例）
- [ ] /api/config
- [ ] /actuator/env
- [ ] /application.properties
- [ ] /config.json
- [ ] （其他：根据目标技术栈扩展）
```

### 文件上传检查清单

> **示例**，包括但不限于以下测试项：

```
## 文件上传测试清单（示例）

### 基础测试（示例）
- [ ] 上传正常文件（.txt, .jpg）
- [ ] 上传脚本文件（.php, .jsp）
- [ ] 上传双重扩展名（.php.jpg）
- [ ] 上传大文件（>1MB）
- [ ] （其他：根据业务允许的文件类型扩展）

### 恶意文件测试（示例）
- [ ] webshell.php
- [ ] webshell.jsp
- [ ] 嵌入XSS的.html
- [ ] 包含路径遍历的文件名
- [ ] （其他：.asp, .aspx, .sh, .py等可执行脚本）

### 验证检查（示例）
- [ ] 未登录时是否允许上传？
- [ ] 登录后是否可以上传任意文件？
- [ ] 文件类型验证是否在服务端？
- [ ] 上传后的文件是否可访问？
- [ ] （其他：文件大小限制、内容过滤、存储路径等）

### 手动验证
- [ ] 尝试访问上传后的文件URL
- [ ] 检查文件是否被重命名
- [ ] 检查文件存储位置
- [ ] 尝试利用文件执行代码
```

---

## 知识库

> **重要**：以下知识库内容为**示例**，包括但不限于所列内容。实际测试中应根据目标特性灵活扩展。

### SQL注入攻击模式

**常见错误消息（示例）**:
- "You have an error in your SQL syntax"
- "Warning: mysql_fetch_array()"
- "ORA-01756: not a valid integer"
- "Unclosed quotation mark"
- （其他：根据目标数据库类型扩展）

**测试Payload（示例）**:
```
# 布尔盲注
' OR '1'='1
' AND '1'='1
' OR 1=1--
（其他：根据注入点位置和上下文调整）

# 时间盲注
' AND SLEEP(5)--
' AND (SELECT COUNT(*) FROM information_schema.tables) > 0--
（其他：BENCHMARK, WAITFOR, pg_sleep等）

# 报错注入
' AND EXTRACTVALUE(1, CONCAT(0x7e, DATABASE()))
' AND updatexml(1, CONCAT(0x7e, VERSION()))
（其他：根据数据库类型扩展）
```

**判断技巧**:
- 响应时间明显不同 → 可能存在时间盲注
- 响应包含SQL错误 → 可能存在报错注入
- 响应完全相同 → 可能被过滤

### JWT攻击模式

**alg: none 攻击（示例）**

原理: 将JWT的签名算法设置为none

测试步骤:
1. 修改JWT header部分: `{"alg": "none", "typ": "JWT"}`
2. 保留payload部分
3. 移除signature部分

判断: 服务器是否接受无效签名

**其他JWT攻击手法（示例）**:
- 算法混淆（使用RS256改为HS256）
- 密钥混淆攻击
- kid参数注入
- jku/x5u URL篡改
- （其他：根据JWT实现特性扩展）

**敏感信息泄露检查（示例）**:
- [ ] payload中是否包含password？
- [ ] payload中是否包含secret？
- [ ] payload中是否包含apiKey？
- [ ] payload中是否包含role/permission等权限字段？
- [ ] （其他：根据业务需求扩展）

### 配置文件常见路径（示例）

```
/api/config
/actuator/env
/application.properties
/config.json
/redis/config
/database
/.git/config
（其他：根据目标技术栈和服务器配置扩展）
```

**敏感信息（示例）**:
- 数据库连接字符串
- API密钥
- 加密密钥
- 第三方凭证
- 云服务AccessKey/SecretKey
- （其他：根据实际配置内容扩展）

---

## 漏洞链构造思维

### 发现用户枚举后的推理

```
你发现的：GET /api/user/check?phone=138xxx 返回 userId

利用链：
1. 收集更多userId → 批量探测手机号
2. 用userId查更多信息 → GET /api/user/info?userId=xxx
3. 尝试修改他人资料 → POST /api/user/update
4. 查看他人订单 → GET /api/order/list?userId=xxx
5. 尝试退款 → POST /api/refund (用他人的orderNo)

最终：用户枚举 → 获取userId → 查订单 → 退款
```

### 发现token泄露后的推理

```
你发现的：{"token": "xxx", "userId": 123}

利用链：
1. token有效吗？ → 用token访问其他接口
2. 能访问admin接口吗？ → GET /api/admin/xxx
3. token能用于其他用户吗？ → 改userId重放

最终：token泄露 → 用token访问敏感接口 → 越权操作
```

### 通用利用链验证格式

```
漏洞利用链：
1. [漏洞A] → [漏洞B] → [漏洞C]
   - 漏洞A: 具体描述
   - 漏洞B: 基于A的结果能做什么
   - 漏洞C: 最终能获取什么

2. 验证记录：
   - Step 1: 执行的操作
   - Step 2: 返回的结果
   - Step 3: 是否成功利用
```

---

## 漏洞验证闭环

### 三步验证流程

```
第一步：发现 (Discover)
  - 发现可疑的响应差异
  - 发现异常的状态码
  - 发现敏感信息暴露

第二步：分析 (Analyze)
  - 多次请求确认差异稳定
  - 对比正常请求和异常请求
  - 检查是否是WAF/路由/认证导致

第三步：验证 (Verify)
  - 确认为漏洞 → 收集证据 → 报告
  - 排除为误报 → 记录原因 → 继续扫描
```

### 验证检查清单（10个维度）

```
□ 维度1: 响应类型 - 是JSON还是HTML？（HTML可能是WAF）
□ 维度2: 状态码 - 是否合理？
□ 维度3: 响应长度 - 是否过短？（可能是拦截）
□ 维度4: WAF拦截 - 是否为WAF/安全设备？
□ 维度5: 敏感信息 - 是否包含password/token/secret？
□ 维度6: 一致性 - 多次请求响应是否一致？
□ 维度7: SQL注入 - 是否包含SQL错误特征？
□ 维度8: IDOR - 是否返回用户/业务数据？
□ 维度9: 认证绕过 - 是否返回token/session？
□ 维度10: 信息泄露 - 是否泄露非公开信息？
```

### 常见误报识别

```
这些不是漏洞（识别为误报）：
1. HTTP 200 返回 HTML 页面
   → 可能是WAF拦截页/SPA路由/默认错误页
   → 验证：是否是JSON格式的业务数据？

2. 响应长度完全相同但返回"登录失效"
   → 说明后端有正确的认证检查
   → 不是漏洞，是安全防护有效

3. 所有ID查询返回相同响应
   → 可能是统一错误处理
   → 验证：是否真的返回了不同的业务数据？
```

---

## 测试执行流程

测试执行分为5个主要阶段，每个阶段有明确的职责和产出。

### 流程概览

```
┌─────────────────────────────────────────────────────────────────────┐
│ 阶段1: 侦察与发现                                                     │
├─────────────────────────────────────────────────────────────────────┤
│ 职责：收集目标信息，发现API端点                                       │
│ 产出：目标技术栈、API端点列表、敏感信息                                │
│ 方法：HTTP探测、JS采集、Swagger发现、Fuzzing                           │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ 阶段2: 分析与分类                                                     │
├─────────────────────────────────────────────────────────────────────┤
│ 职责：分析已发现端点的类型和特征                                       │
│ 产出：端点分类（认证类/用户类/订单类等）、测试优先级                   │
│ 方法：路径分析、参数分析、响应分析                                     │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ 阶段3: 漏洞测试                                                      │
├─────────────────────────────────────────────────────────────────────┤
│ 职责：根据端点类型执行针对性测试                                       │
│ 产出：漏洞清单（含证据）                                              │
│ 方法：SQL注入测试、IDOR测试、JWT测试、业务逻辑测试                     │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ 阶段4: 验证与确认                                                     │
├─────────────────────────────────────────────────────────────────────┤
│ 职责：验证发现的问题是否为真实漏洞，排除误报                           │
│ 产出：确认漏洞列表、误报记录                                          │
│ 方法：10维度验证、响应对比分析                                         │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ 阶段5: 利用与报告                                                     │
├─────────────────────────────────────────────────────────────────────┤
│ 职责：构造利用链，生成测试报告                                        │
│ 产出：利用链、完整报告                                                │
│ 方法：漏洞关联分析、修复建议                                           │
└─────────────────────────────────────────────────────────────────────┘
```

---

### 阶段1: 侦察与发现

#### 职责
- 探测目标可访问性
- 识别技术栈（SPA/传统）
- 发现API端点
- 收集敏感信息

#### 操作步骤

**1.1 基础探测**
```
- HTTP/HTTPS探测目标可访问性
- 检查响应头Server字段
- 检查HTML特征（Vue/React/Angular）
- 判断是否为SPA应用
```

**1.2 SPA应用识别**
```
判断依据：
- /api/* 返回HTML → 可能是SPA
- HTML包含JS chunk路径 → Vue/React应用
- 响应头Server字段 → nginx反向代理

如果不是SPA：
→ 跳过JS采集阶段
→ 直接进入API发现
```

**1.3 JS采集（SPA必须执行）**

> **【强制】必须使用Playwright，禁止降级**

```
采集步骤：
1. 启动Playwright - ignore_https_errors=True
2. 访问目标 - wait_until=networkidle
3. 等待加载 - page.wait_for_timeout(5000)
4. 拦截请求 - page.on('request')捕获XHR/Fetch
5. 用户交互 - 点击、滚动、表单填写触发动态API
6. 采集敏感信息 - cookies、localStorage、响应头

【禁止降级】绝对不允许：
- 不能降级到 selenium
- 不能降级到 pyppeteer
- 不能降级到 requests 静态解析
```

**1.4 JS深度分析（AST+正则双模式）**

> **【关键】必须使用AST+正则双模式进行深度分析**

```
1. 提取baseURL配置（最优先！）
   patterns:
   - r'baseURL\s*[:=]\s*["\']([^"\']+)["\']'
   - r'axios\.create\s*\(\s*\{([^}]+)\}'
   
   重要发现：
   - baseURL:"" 为空 → 使用相对路径 + nginx代理
   - baseURL:"https://api.xxx.com" → 使用配置的域名前缀
   - baseURL不存在 → 使用同源请求

2. 正则模式（快速提取）
   patterns:
   - r'["\'](/(?:user|auth|admin|login|logout|api|v\d|frame)[^"\']*)["\']'
   - r'axios\.[a-z]+\(["\']([^"\']+)["\']'
   - r'fetch\(["\']([^"\']+)["\']'
   - r'\.get\(["\']([^"\']+)["\']'
   - r'\.post\(["\']([^"\']+)["\']'
   
3. 【重要】递归分析所有 chunk 文件
   - webpack 打包的应用会将代码分散到多个 chunk 文件中
   - 大 chunk 文件（>50KB）通常包含更多业务逻辑

4. 敏感信息提取
   - IP地址、域名、凭证信息
```

**1.5 API端点发现**
```
发现方法：
- Swagger探测：/swagger-ui.html, /v2/api-docs
- Fuzzing：使用前缀字典 + 端点字典组合测试
- 响应分析：从API响应中发现更多端点
```

#### 产出
- 目标技术栈类型
- API端点完整列表
- base_path配置
- 敏感信息泄露

---

### 阶段2: 分析与分类

#### 职责
- 分析每个API端点的功能和特征
- 判断端点类型
- 确定测试优先级

#### 端点分类

| 类型 | 路径特征 | 测试重点 |
|------|----------|----------|
| 认证类 | /login, /auth, /token | SQL注入、暴力破解、用户枚举 |
| 用户类 | /user, /profile, /info | IDOR、信息泄露 |
| 订单类 | /order, /pay, /refund | 金额篡改、越权 |
| 文件类 | /upload, /download, /file | 上传绕过、路径遍历 |
| 管理类 | /admin, /manage | 垂直越权、权限绕过 |
| 配置类 | /config, /settings | 信息泄露 |

#### 响应类型判断

| 响应类型 | 特征 | 含义 |
|----------|------|------|
| JSON对象 | `{"code":200,"data":{...}}` | 真实API响应 |
| JSON数组 | `[{"id":1,...}]` | 真实数据列表 |
| HTML页面 | `<!DOCTYPE html>...` | SPA路由/WAF/错误页 |
| 空响应 | 长度<50字节 | 可能是错误/空数据 |
| 重定向 | HTTP 301/302 | 需要认证/跳转 |

---

### 阶段3: 漏洞测试

#### 职责
- 根据端点类型执行针对性测试
- 发现即测，不等待

#### 发现即测原则

> **不要等全部扫描完再测试，发现接口立即测试。**

```
发现Swagger → 立即访问获取更多API
发现Actuator → 立即测试敏感端点
发现登录接口 → 立即测试注入/爆破
发现查询接口 → 立即测试IDOR
发现文件上传接口 → 立即测试上传绕过
```

#### 测试类型

**SQL注入测试**
- 测试点：所有用户输入点（参数、Header、Cookie）
- 方法：布尔盲注、时间盲注、报错注入

**越权测试（IDOR）**
- 测试点：资源访问接口
- 方法：修改资源ID测试访问控制

**认证测试**
- 测试点：登录、注册、Token相关接口
- 方法：弱密码、暴力破解、JWT算法攻击

**敏感信息泄露**
- 测试点：所有接口响应
- 方法：检查是否包含密码、Token、内部地址等

**业务逻辑测试**
- 测试点：订单、支付、退款接口
- 方法：金额篡改、流程绕过

---

### 阶段4: 验证与确认

#### 职责
- 验证发现的问题是否为真实漏洞
- 排除误报
- 确认漏洞优先级

#### 三步验证流程

```
第一步：发现 (Discover)
  - 发现可疑的响应差异
  - 发现异常的状态码
  - 发现敏感信息暴露

第二步：分析 (Analyze)
  - 多次请求确认差异稳定
  - 对比正常请求和异常请求
  - 检查是否是WAF/路由/认证导致

第三步：验证 (Verify)
  - 确认为漏洞 → 收集证据 → 报告
  - 排除为误报 → 记录原因 → 继续扫描
```

#### 10维度验证清单

```
□ 维度1: 响应类型 - 是JSON还是HTML？（HTML可能是WAF）
□ 维度2: 状态码 - 是否合理？
□ 维度3: 响应长度 - 是否过短？（可能是拦截）
□ 维度4: WAF拦截 - 是否为WAF/安全设备？
□ 维度5: 敏感信息 - 是否包含password/token/secret？
□ 维度6: 一致性 - 多次请求响应是否一致？
□ 维度7: SQL注入 - 是否包含SQL错误特征？
□ 维度8: IDOR - 是否返回用户/业务数据？
□ 维度9: 认证绕过 - 是否返回token/session？
□ 维度10: 信息泄露 - 是否泄露非公开信息？
```

#### 常见误报识别

```
这些不是漏洞（识别为误报）：
1. HTTP 200 返回 HTML 页面
   → 可能是WAF拦截页/SPA路由/默认错误页
   → 验证：是否是JSON格式的业务数据？

2. 响应长度完全相同但返回"登录失效"
   → 说明后端有正确的认证检查
   → 不是漏洞，是安全防护有效

3. 所有ID查询返回相同响应
   → 可能是统一错误处理
   → 验证：是否真的返回了不同的业务数据？
```

---

### 阶段5: 利用与报告

#### 职责
- 构造漏洞利用链
- 评估漏洞影响
- 生成测试报告

#### 利用链构造

```
发现用户枚举后的推理：
用户枚举 → 获取userId → 查订单 → 退款

发现token泄露后的推理：
token泄露 → 用token访问敏感接口 → 越权操作
```

#### 报告内容

```
1. 测试概要
   - 测试目标、范围、时间

2. 漏洞列表
   - 漏洞类型、位置、证据、复现步骤

3. 验证维度详情
   - 每个漏洞的10维度验证结果

4. 修复建议
   - 针对每个漏洞的修复方案
```

---

## Fuzzing策略

> **Fuzzing用于发现未知的API端点**，当自动化采集不充分时使用。

### API前缀字典

> **示例**，包括但不限于：

```python
api_prefixes = [
    # 通用
    "/api", "/api/v1", "/api/v2", "/api/v3", "/api/v4",
    "/rest", "/rest/api", "/webapi",
    # 认证
    "/auth", "/oauth", "/oauth2", "/cas", "/sso",
    # 管理
    "/admin", "/admin/api", "/manager", "/backend",
    # 协议
    "/openapi", "/open/api", "/gateway", "/proxy",
]
```

### API端点字典

> **示例**，包括但不限于：

```python
api_endpoints = [
    # 通用 CRUD
    "login", "logout", "register", "list", "add", "delete", "modify",
    "getList", "getListOfPage", "detail", "getInfo", "profile",
    # 用户相关
    "user", "user/list", "user/add", "user/delete", "user/modify",
    "user/profile", "user/restPassword", "user/enable", "user/disable",
    # 角色权限
    "role", "role/list", "role/add", "role/delete", "role/modify",
    "menu", "menu/list", "menu/add", "menu/delete", "menu/modify",
    # 文件操作
    "file", "upload", "download", "import", "export",
    "imgUpload", "avatar", "attachment",
]
```

### 业务端点扩展模式

> **根据发现的API模式扩展测试**：

```
发现的模式: /{module}/{operation}
可能存在的端点:
- /{module}/list          → 列表查询
- /{module}/add          → 新增创建
- /{module}/modify       → 修改更新
- /{module}/delete       → 删除操作
- /{module}/detail       → 详情查看
- /{module}/getInfo      → 信息获取
- /{module}/export       → 导出数据
- /{module}/import       → 导入数据

发现的模式: /{resource}/{id}
可能存在的端点:
- GET  /{resource}/{id}     → 获取详情
- PUT  /{resource}/{id}     → 完整更新
- DELETE /{resource}/{id}   → 删除资源
- PATCH /{resource}/{id}   → 部分更新
```

---

## 常见漏洞模式识别

### 用户相关漏洞模式

```
1. 用户信息泄露
   特征：响应包含password、token
   测试：不带认证访问

2. 用户枚举
   特征：用户存在/不存在响应不同
   测试：探测不存在的手机号/邮箱

3. 密码重置漏洞
   特征：可通过phone/email重置
   测试：尝试修改他人密码

4. 越权访问
   特征：通过参数切换用户
   测试：修改userId/phone等参数
```

### 订单相关漏洞模式

```
1. 订单遍历
   特征：参数化查询订单
   测试：修改userId查他人订单

2. 订单篡改
   特征：订单金额可修改
   测试：尝试amount=0.01

3. 虚假订单
   特征：可创建任意订单
   测试：构造恶意订单数据

4. 退款绕过
   特征：退款接口无校验
   测试：使用他人orderNo退款
```

### 认证相关漏洞模式

```
1. JWT伪造
   特征：alg:None 或不验签
   测试：修改payload重放

2. 暴力破解
   特征：无验证码、无限流
   测试：多次尝试密码

3. 会话固定
   特征：登录后session不变
   测试：登录前后cookie对比

4. 登出后令牌仍有效
   特征：token注销机制缺失
   测试：登出后重放token
```

---

## HTTP方法与测试策略

### 不同方法的测试重点

| 方法 | 测试重点 |
|------|----------|
| GET | 参数遍历、IDOR，信息泄露 |
| POST | 认证绕过、业务逻辑、注入 |
| PUT | 资源篡改、越权修改 |
| DELETE | 资源删除、越权删除 |
| PATCH | 部分更新、字段覆盖 |

### 参数测试思维

```
接口：GET /api/xxx?param=value

测试顺序：
1. param=空值
2. param=正常值
3. param=特殊字符 (' " < >)
4. param=SQL注入 (1' OR '1'='1)
5. param=XSS (<script>alert(1)</script>)
6. param=路径遍历 (../../../etc/passwd)
7. param=其他用户的值 (IDOR)
```

---

## 认证上下文理解

### 发现登录接口后

```
你发现的：POST /api/login {"username":"xxx","password":"xxx"}

思考：
1. 返回token吗？ → 记录token
2. 返回userId吗？ → 记录userId
3. 响应有什么区别？ → 用户枚举
4. 有验证码吗？ → 暴力破解难度

接下来用这个token：
- 访问 GET /api/user/info
- 访问 GET /api/order/list
- 尝试 GET /api/admin/xxx (测试权限)
```

### 发现token但不知道用法时

```
你发现的：token=eyJhbGciOiJIUzI1NiJ9...

思考：
1. JWT吗？ → 解码看payload
2. 放在哪？ → Authorization: Bearer token
3. 哪个接口用？ → 尝试访问需要认证的接口
4. userId是什么？ → 从token解码获取
```

---

## 利用链推理提示词

当发现多个漏洞时，按以下模板进行利用链分析：

### 模板1：认证类漏洞利用链

```
发现的漏洞：
- 登录接口无验证码
- 暴力破解可获取有效账号

利用链推理：
1. 使用暴力破解获取有效账号 → 记录账号密码
2. 使用有效账号登录 → 获取 JWT Token
3. 使用 Token 访问敏感接口 → 测试越权操作
4. 越权访问获取更多数据 → 扩大攻击面

验证步骤：
- 使用获取的 Token 请求 /api/admin/user/list
- 如果返回用户数据，说明 Token 有效且可利用
```

### 模板2：用户枚举 + IDOR 利用链

```
发现的漏洞：
- 用户存在/不存在响应不同（用户枚举）
- 查询接口存在 IDOR

利用链推理：
1. 用户枚举获取 userId 列表
2. 使用 userId 遍历查询接口
3. 如果能查到他人数据，说明存在 IDOR

验证步骤：
- 使用不同 userId 请求 /api/user/profile?userId=X
- 对比返回数据是否属于不同用户
```

### 模板3：敏感信息泄露 + 认证绕过

```
发现的漏洞：
- 某接口返回敏感信息（如 password）
- 另一接口存在认证绕过

利用链推理：
1. 认证绕过获取初始访问
2. 利用敏感信息泄露获取更多凭证
3. 组合利用扩大攻击面

验证步骤：
- 绕过认证访问 /api/info 获取数据
- 检查响应中包含可利用的凭证
```

---

## 特殊情况处理

### 遇到WAF/安全设备时

```
识别特征：
- 所有请求返回相似的HTML页面
- 响应包含"拦截"、"安全"、"访问受限"等关键词
- 响应内容与实际API无关

处理方法：
1. 识别为WAF拦截，不是漏洞
2. 记录"存在WAF防护"作为安全能力
3. 可以尝试降低请求频率绕过

判断逻辑：
- 请求1: 返回业务JSON = 正常
- 请求2: 返回HTML拦截页 = WAF
- 请求3: 返回业务JSON = 恢复
```

### 遇到SPA应用时

```
识别特征：
- /api/* 路径返回HTML页面
- 响应内容是前端框架代码

处理方法：
1. 通过JS源码分析获取真实API配置
2. 使用无头浏览器触发动态API请求
3. 不要对SPA路由的/api/*路径直接测试

判断逻辑：
- GET /api/user/info 返回HTML = SPA前端路由
- GET /api/user/info 返回JSON = 真实API
```

### 遇到验证码/限流时

```
思考：
- 验证码能绕过吗？ → 改参数、删cookie
- 限流能绕过吗？ → 改IP、延时
- 有风控吗？ → 行为异常检测
```

### 遇到加密/混淆的数据时

```
思考：
- 能解密吗？ → 查看前端JS代码
- 有密钥泄露吗？ → 检查响应、注释
- 能绕过吗？ → 不带加密参数试试
```

---

## 测试记录系统

### 记录格式模板

#### 假设记录
```
时间: 2026-04-06 22:45:00
假设: 登录接口可能存在SQL注入
依据: 响应时间差异
状态: 待验证
```

#### 测试记录
```
时间: 2026-04-06 22:46:00
测试: POST /api/login
Payload: {"username": "admin'", "password": "test"}
结果: HTTP 500
状态: 需要服务器日志确认
```

#### 发现记录
```
时间: 2026-04-06 22:47:00
类型: 信息泄露
端点: /api/config
详情: 暴露内网IP 192.168.1.100
严重程度: 中等
```

---

## 参考资源

### references/ 参考文档

| 文档 | 内容 | 使用阶段 |
|------|------|----------|
| `workflows.md` | 完整扫描流程参考 | 全流程 |
| `rest-guidance.md` | REST API 测试指导 | 测试阶段 |
| `graphql-guidance.md` | GraphQL 测试指导 | 测试阶段 |
| `test-matrix.md` | 测试矩阵（方法选择） | 测试阶段 |
| `validation.md` | 漏洞验证标准 | 验证阶段 |
| `severity-model.md` | CVSS 严重性分级 | 报告阶段 |

### references/vulnerabilities/ 漏洞知识库

| 文档 | 内容 | 对应测试 |
|------|------|----------|
| `01-sqli-tests.md` | SQL注入测试方法与Payload | SQL注入测试 |
| `02-user-enum-tests.md` | 用户枚举测试方法 | 用户枚举测试 |
| `03-jwt-tests.md` | JWT认证测试方法 | JWT测试 |
| `04-idor-tests.md` | IDOR越权测试 | 越权测试 |
| `05-sensitive-data-tests.md` | 敏感信息泄露测试 | 信息泄露测试 |
| `06-biz-logic-tests.md` | 业务逻辑漏洞测试 | 业务逻辑测试 |
| `07-security-config-tests.md` | 安全配置测试 | 安全配置测试 |
| `08-brute-force-tests.md` | 暴力破解测试 | 暴力破解测试 |
| `09-vulnerability-chains.md` | 漏洞关联与利用链 | 利用链构造 |
| `10-auth-tests.md` | 认证测试扩展 | 认证测试 |
| `11-graphql-tests.md` | GraphQL测试 | GraphQL测试 |
| `12-ssrf-tests.md` | SSRF测试 | SSRF测试 |

### core/ 核心模块

| 类别 | 模块 | 功能 | 使用阶段 |
|------|------|------|----------|
| **采集** | `browser_collect.py` | Playwright无头浏览器采集 | 侦察阶段 |
| **采集** | `js_parser.py` | JS源码解析（AST+正则） | 侦察阶段 |
| **采集** | `http_client.py` | 快速HTTP探测 | 侦察阶段 |
| **分析** | `sensitive_finder.py` | 敏感信息发现 | 分析阶段 |
| **分析** | `response_analyzer.py` | 响应类型分析 | 分析阶段 |
| **测试** | `sqli_tester.py` | SQL注入测试 | 测试阶段 |
| **测试** | `idor_tester.py` | 越权测试 | 测试阶段 |
| **测试** | `jwt_tester.py` | JWT漏洞测试 | 测试阶段 |
| **测试** | `auth_tester.py` | 认证绕过测试 | 测试阶段 |
| **验证** | `vuln_verifier.py` | 10维度漏洞验证 | 验证阶段 |
| **辅助** | `base_path_dict.py` | API base path字典 | 侦察阶段 |
| **辅助** | `payload_lib.py` | Payload库 | 测试阶段 |
| **编排** | `orchestrator.py` | 多阶段测试编排 | 全流程 |

### 场景→模块选择

| 场景 | 推荐模块 |
|------|----------|
| SPA应用发现API | `browser_collect.py` + `js_parser.py` |
| 快速探测目标 | `http_client.py` |
| SQL注入测试 | `sqli_tester.py` |
| 越权测试 | `idor_tester.py` |
| JWT漏洞测试 | `jwt_tester.py` |
| 漏洞验证 | `vuln_verifier.py` |
| 全面扫描 | `orchestrator.py` |

---

## 前置检查与依赖处理

### 发现即测原则

**不要等全部扫描完再测试，发现接口立即测试。**

```
错误做法：
1. 先扫描所有端点
2. 再逐一测试漏洞

正确做法：
1. 发现一个接口 → 立即测试相关漏洞
2. 发现敏感接口 → 立即深入测试
3. 发现认证接口 → 立即测试绕过
```

### API 类型对应的测试重点

| API 类型 | 发现后立即测试 |
|----------|---------------|
| 认证类 (login, auth) | SQL注入、暴力破解、用户枚举 |
| 查询类 (list, get, search) | IDOR，信息泄露、注入 |
| 操作类 (add, modify, delete) | 越权、批量操作、业务逻辑 |
| 文件类 (upload, download) | 上传绕过、恶意文件、路径遍历 |
| 支付类 (pay, refund, order) | 金额篡改、支付绕过、退款欺诈 |

### 依赖问题处理

```
遇到Playwright不可用：
1. pip install playwright && playwright install chromium
2. playwright install-deps chromium
3. 尝试MCP工具: headless_browser
4. 【禁止降级到selenium/requests】
```
